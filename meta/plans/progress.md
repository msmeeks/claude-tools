# Iteration progress

- 2026-07-17 16:05 — `fix-sdlc-skill-docs.md` (#44, #49, #50, #51): Markdown-only fixes aligning the planning/close/triage skills with the incremental-review-gate contract. #44: `close-iteration` Step 7 now builds the `Closes` list from `plans[?status=="done"].issues` + `sdlc_finding_issues` only (never stalled-plan issues), with a new Step 3 "stalled plans with open issues" warning and a Step 10 `Issues left open:` report line. #51: Step 2b documents the legitimate `complete → pending → complete` cycle across re-armed rounds (pass condition unchanged). #49: `plan-iteration` Step 7 and `triage-pr-comments` Step 6 both instruct preserving all eight top-level prd.json keys (`last_reviewed_sha`, `sdlc_review_status`, `sdlc_finding_issues`, `sdlc_review_completed_agents`, `pr_number`, `prd_issue`, `feature_branches`, `smoke_test`), modifying only `plans[]`. #50: `triage-pr-comments` Case 3 now states the entry-level `pending`/`attempts:0` reset is sufficient and must not touch `sdlc_review_status` (runner re-arms; manual reset hits the `save_prd` latch or desyncs round state). No code changed; full suite (108) + ruff green.
- 2026-07-17 15:47 — `fix-runner-findings-lifecycle.md` (#47, #48): Fixed two cross-round bugs in the SDLC review gate (`scripts/run-next-plan.py`). #48: added `_rotate_findings_file` — a path-safe (symlink-refusing, repo-root-scoped) unlink of `meta/sdlc-review-findings.md` run once at each fresh round's start (guarded on `sdlc_review_completed_agents` being empty, so a mid-round resume keeps its appended findings), preventing a prior round's findings from being re-filed as duplicate issues. #47: `sdlc_finding_issues` is now iteration-cumulative (append + dedupe in `_run_file_issues_phase`, never reset at round end / empty-increment / re-arm) so `/close-iteration` sees the whole set; the per-round "already filed" guard moved to a new `sdlc_round_filed_issues` scratch field (validated in `_validate_prd_schema`). Triage runs against the round scratch only. New tests in `test_sdlc_gate.py` cover scratch-field validation, cumulative append + dedupe, re-armed-round re-file, within-round re-file guard, findings-file rotation (fresh-round vs mid-round-resume + symlink refusal), and the terminal-guard-with-scratch-field case. Full suite (108) + ruff green. Updated `docs/features/run-next-plan.md`.
- 2026-07-17 15:40 — `fix-runner-attempts-accounting.md` (#45, #46): Deferred plan attempt accounting until after the Claude session in `scripts/run-next-plan.py`. Added `account_attempt` + `_attribute_worked_plan`: a `rate_limit` outcome now charges nothing (#46), and the attempt is charged to the plan the session demonstrably advanced — the one flipped to `done`, or a validated `<plan>slug.md</plan>` sigil — never `eligible[0]` by default (#45). `mark_stalled` can only fire for a plan a session actually ran; model escalation still triggers on the same attempt via `selected["attempts"] + 1`. Added `<plan>` sigil instruction to the session prompt. New tests in `test_orchestration.py` cover rate-limit no-charge, done-flip attribution, sigil fallback + ineligible-sigil rejection, no-guess default, stall-on-exceed, and unrun-plan stall guard. Full suite (98) + ruff green. Updated `docs/features/run-next-plan.md`.
